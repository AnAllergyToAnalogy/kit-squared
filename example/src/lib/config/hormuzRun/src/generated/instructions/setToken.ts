/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findGamePda } from "../pdas";
import { HORMUZ_RUN_PROGRAM_ADDRESS } from "../programs";

export const SET_TOKEN_DISCRIMINATOR = new Uint8Array([
  22, 161, 71, 245, 113, 41, 215, 234,
]);

export function getSetTokenDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SET_TOKEN_DISCRIMINATOR);
}

export type SetTokenInstruction<
  TProgram extends string = typeof HORMUZ_RUN_PROGRAM_ADDRESS,
  TAccountGame extends string | AccountMeta<string> = string,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountGameTokenAccount extends string | AccountMeta<string> = string,
  TAccountCreatorTokenAccount extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountGame extends string
        ? WritableAccount<TAccountGame>
        : TAccountGame,
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      TAccountMint extends string
        ? WritableAccount<TAccountMint>
        : TAccountMint,
      TAccountGameTokenAccount extends string
        ? WritableAccount<TAccountGameTokenAccount>
        : TAccountGameTokenAccount,
      TAccountCreatorTokenAccount extends string
        ? WritableAccount<TAccountCreatorTokenAccount>
        : TAccountCreatorTokenAccount,
      ...TRemainingAccounts,
    ]
  >;

export type SetTokenInstructionData = {
  discriminator: ReadonlyUint8Array;
  tokenAccount: Address;
  tokenProgram: Address;
};

export type SetTokenInstructionDataArgs = {
  tokenAccount: Address;
  tokenProgram: Address;
};

export function getSetTokenInstructionDataEncoder(): FixedSizeEncoder<SetTokenInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["tokenAccount", getAddressEncoder()],
      ["tokenProgram", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_TOKEN_DISCRIMINATOR }),
  );
}

export function getSetTokenInstructionDataDecoder(): FixedSizeDecoder<SetTokenInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["tokenAccount", getAddressDecoder()],
    ["tokenProgram", getAddressDecoder()],
  ]);
}

export function getSetTokenInstructionDataCodec(): FixedSizeCodec<
  SetTokenInstructionDataArgs,
  SetTokenInstructionData
> {
  return combineCodec(
    getSetTokenInstructionDataEncoder(),
    getSetTokenInstructionDataDecoder(),
  );
}

export type SetTokenAsyncInput<
  TAccountGame extends string = string,
  TAccountSigner extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountMint extends string = string,
  TAccountGameTokenAccount extends string = string,
  TAccountCreatorTokenAccount extends string = string,
> = {
  game?: Address<TAccountGame>;
  signer: TransactionSigner<TAccountSigner>;
  systemProgram?: Address<TAccountSystemProgram>;
  /** Token Accounts */
  tokenProgram?: Address<TAccountTokenProgram>;
  mint: Address<TAccountMint>;
  gameTokenAccount: Address<TAccountGameTokenAccount>;
  creatorTokenAccount: Address<TAccountCreatorTokenAccount>;
  tokenAccount: SetTokenInstructionDataArgs["tokenAccount"];
  tokenProgramArg: SetTokenInstructionDataArgs["tokenProgram"];
};

export async function getSetTokenInstructionAsync<
  TAccountGame extends string,
  TAccountSigner extends string,
  TAccountSystemProgram extends string,
  TAccountTokenProgram extends string,
  TAccountMint extends string,
  TAccountGameTokenAccount extends string,
  TAccountCreatorTokenAccount extends string,
  TProgramAddress extends Address = typeof HORMUZ_RUN_PROGRAM_ADDRESS,
>(
  input: SetTokenAsyncInput<
    TAccountGame,
    TAccountSigner,
    TAccountSystemProgram,
    TAccountTokenProgram,
    TAccountMint,
    TAccountGameTokenAccount,
    TAccountCreatorTokenAccount
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SetTokenInstruction<
    TProgramAddress,
    TAccountGame,
    TAccountSigner,
    TAccountSystemProgram,
    TAccountTokenProgram,
    TAccountMint,
    TAccountGameTokenAccount,
    TAccountCreatorTokenAccount
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? HORMUZ_RUN_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    game: { value: input.game ?? null, isWritable: true },
    signer: { value: input.signer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    mint: { value: input.mint ?? null, isWritable: true },
    gameTokenAccount: {
      value: input.gameTokenAccount ?? null,
      isWritable: true,
    },
    creatorTokenAccount: {
      value: input.creatorTokenAccount ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input, tokenProgram: input.tokenProgramArg };

  // Resolve default values.
  if (!accounts.game.value) {
    accounts.game.value = await findGamePda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("game", accounts.game),
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("systemProgram", accounts.systemProgram),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("mint", accounts.mint),
      getAccountMeta("gameTokenAccount", accounts.gameTokenAccount),
      getAccountMeta("creatorTokenAccount", accounts.creatorTokenAccount),
    ],
    data: getSetTokenInstructionDataEncoder().encode(
      args as SetTokenInstructionDataArgs,
    ),
    programAddress,
  } as SetTokenInstruction<
    TProgramAddress,
    TAccountGame,
    TAccountSigner,
    TAccountSystemProgram,
    TAccountTokenProgram,
    TAccountMint,
    TAccountGameTokenAccount,
    TAccountCreatorTokenAccount
  >);
}

export type SetTokenInput<
  TAccountGame extends string = string,
  TAccountSigner extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountMint extends string = string,
  TAccountGameTokenAccount extends string = string,
  TAccountCreatorTokenAccount extends string = string,
> = {
  game: Address<TAccountGame>;
  signer: TransactionSigner<TAccountSigner>;
  systemProgram?: Address<TAccountSystemProgram>;
  /** Token Accounts */
  tokenProgram?: Address<TAccountTokenProgram>;
  mint: Address<TAccountMint>;
  gameTokenAccount: Address<TAccountGameTokenAccount>;
  creatorTokenAccount: Address<TAccountCreatorTokenAccount>;
  tokenAccount: SetTokenInstructionDataArgs["tokenAccount"];
  tokenProgramArg: SetTokenInstructionDataArgs["tokenProgram"];
};

export function getSetTokenInstruction<
  TAccountGame extends string,
  TAccountSigner extends string,
  TAccountSystemProgram extends string,
  TAccountTokenProgram extends string,
  TAccountMint extends string,
  TAccountGameTokenAccount extends string,
  TAccountCreatorTokenAccount extends string,
  TProgramAddress extends Address = typeof HORMUZ_RUN_PROGRAM_ADDRESS,
>(
  input: SetTokenInput<
    TAccountGame,
    TAccountSigner,
    TAccountSystemProgram,
    TAccountTokenProgram,
    TAccountMint,
    TAccountGameTokenAccount,
    TAccountCreatorTokenAccount
  >,
  config?: { programAddress?: TProgramAddress },
): SetTokenInstruction<
  TProgramAddress,
  TAccountGame,
  TAccountSigner,
  TAccountSystemProgram,
  TAccountTokenProgram,
  TAccountMint,
  TAccountGameTokenAccount,
  TAccountCreatorTokenAccount
> {
  // Program address.
  const programAddress = config?.programAddress ?? HORMUZ_RUN_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    game: { value: input.game ?? null, isWritable: true },
    signer: { value: input.signer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    mint: { value: input.mint ?? null, isWritable: true },
    gameTokenAccount: {
      value: input.gameTokenAccount ?? null,
      isWritable: true,
    },
    creatorTokenAccount: {
      value: input.creatorTokenAccount ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input, tokenProgram: input.tokenProgramArg };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("game", accounts.game),
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("systemProgram", accounts.systemProgram),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("mint", accounts.mint),
      getAccountMeta("gameTokenAccount", accounts.gameTokenAccount),
      getAccountMeta("creatorTokenAccount", accounts.creatorTokenAccount),
    ],
    data: getSetTokenInstructionDataEncoder().encode(
      args as SetTokenInstructionDataArgs,
    ),
    programAddress,
  } as SetTokenInstruction<
    TProgramAddress,
    TAccountGame,
    TAccountSigner,
    TAccountSystemProgram,
    TAccountTokenProgram,
    TAccountMint,
    TAccountGameTokenAccount,
    TAccountCreatorTokenAccount
  >);
}

export type ParsedSetTokenInstruction<
  TProgram extends string = typeof HORMUZ_RUN_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    game: TAccountMetas[0];
    signer: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
    /** Token Accounts */
    tokenProgram: TAccountMetas[3];
    mint: TAccountMetas[4];
    gameTokenAccount: TAccountMetas[5];
    creatorTokenAccount: TAccountMetas[6];
  };
  data: SetTokenInstructionData;
};

export function parseSetTokenInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetTokenInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 7,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      game: getNextAccount(),
      signer: getNextAccount(),
      systemProgram: getNextAccount(),
      tokenProgram: getNextAccount(),
      mint: getNextAccount(),
      gameTokenAccount: getNextAccount(),
      creatorTokenAccount: getNextAccount(),
    },
    data: getSetTokenInstructionDataDecoder().decode(instruction.data),
  };
}
