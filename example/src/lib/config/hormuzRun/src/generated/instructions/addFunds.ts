/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findGamePda } from "../pdas";
import { HORMUZ_RUN_PROGRAM_ADDRESS } from "../programs";

export const ADD_FUNDS_DISCRIMINATOR = new Uint8Array([
  132, 237, 76, 57, 80, 10, 179, 138,
]);

export function getAddFundsDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(ADD_FUNDS_DISCRIMINATOR);
}

export type AddFundsInstruction<
  TProgram extends string = typeof HORMUZ_RUN_PROGRAM_ADDRESS,
  TAccountGame extends string | AccountMeta<string> = string,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountGameTokenAccount extends string | AccountMeta<string> = string,
  TAccountPlayerTokenAccount extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountGame extends string
        ? WritableAccount<TAccountGame>
        : TAccountGame,
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      TAccountMint extends string
        ? WritableAccount<TAccountMint>
        : TAccountMint,
      TAccountGameTokenAccount extends string
        ? WritableAccount<TAccountGameTokenAccount>
        : TAccountGameTokenAccount,
      TAccountPlayerTokenAccount extends string
        ? WritableAccount<TAccountPlayerTokenAccount>
        : TAccountPlayerTokenAccount,
      ...TRemainingAccounts,
    ]
  >;

export type AddFundsInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
};

export type AddFundsInstructionDataArgs = { amount: number | bigint };

export function getAddFundsInstructionDataEncoder(): FixedSizeEncoder<AddFundsInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amount", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: ADD_FUNDS_DISCRIMINATOR }),
  );
}

export function getAddFundsInstructionDataDecoder(): FixedSizeDecoder<AddFundsInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amount", getU64Decoder()],
  ]);
}

export function getAddFundsInstructionDataCodec(): FixedSizeCodec<
  AddFundsInstructionDataArgs,
  AddFundsInstructionData
> {
  return combineCodec(
    getAddFundsInstructionDataEncoder(),
    getAddFundsInstructionDataDecoder(),
  );
}

export type AddFundsAsyncInput<
  TAccountGame extends string = string,
  TAccountSigner extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountMint extends string = string,
  TAccountGameTokenAccount extends string = string,
  TAccountPlayerTokenAccount extends string = string,
> = {
  game?: Address<TAccountGame>;
  signer: TransactionSigner<TAccountSigner>;
  systemProgram?: Address<TAccountSystemProgram>;
  /** Token */
  tokenProgram?: Address<TAccountTokenProgram>;
  mint: Address<TAccountMint>;
  gameTokenAccount: Address<TAccountGameTokenAccount>;
  playerTokenAccount: Address<TAccountPlayerTokenAccount>;
  amount: AddFundsInstructionDataArgs["amount"];
};

export async function getAddFundsInstructionAsync<
  TAccountGame extends string,
  TAccountSigner extends string,
  TAccountSystemProgram extends string,
  TAccountTokenProgram extends string,
  TAccountMint extends string,
  TAccountGameTokenAccount extends string,
  TAccountPlayerTokenAccount extends string,
  TProgramAddress extends Address = typeof HORMUZ_RUN_PROGRAM_ADDRESS,
>(
  input: AddFundsAsyncInput<
    TAccountGame,
    TAccountSigner,
    TAccountSystemProgram,
    TAccountTokenProgram,
    TAccountMint,
    TAccountGameTokenAccount,
    TAccountPlayerTokenAccount
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  AddFundsInstruction<
    TProgramAddress,
    TAccountGame,
    TAccountSigner,
    TAccountSystemProgram,
    TAccountTokenProgram,
    TAccountMint,
    TAccountGameTokenAccount,
    TAccountPlayerTokenAccount
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? HORMUZ_RUN_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    game: { value: input.game ?? null, isWritable: true },
    signer: { value: input.signer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    mint: { value: input.mint ?? null, isWritable: true },
    gameTokenAccount: {
      value: input.gameTokenAccount ?? null,
      isWritable: true,
    },
    playerTokenAccount: {
      value: input.playerTokenAccount ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.game.value) {
    accounts.game.value = await findGamePda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("game", accounts.game),
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("systemProgram", accounts.systemProgram),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("mint", accounts.mint),
      getAccountMeta("gameTokenAccount", accounts.gameTokenAccount),
      getAccountMeta("playerTokenAccount", accounts.playerTokenAccount),
    ],
    data: getAddFundsInstructionDataEncoder().encode(
      args as AddFundsInstructionDataArgs,
    ),
    programAddress,
  } as AddFundsInstruction<
    TProgramAddress,
    TAccountGame,
    TAccountSigner,
    TAccountSystemProgram,
    TAccountTokenProgram,
    TAccountMint,
    TAccountGameTokenAccount,
    TAccountPlayerTokenAccount
  >);
}

export type AddFundsInput<
  TAccountGame extends string = string,
  TAccountSigner extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountMint extends string = string,
  TAccountGameTokenAccount extends string = string,
  TAccountPlayerTokenAccount extends string = string,
> = {
  game: Address<TAccountGame>;
  signer: TransactionSigner<TAccountSigner>;
  systemProgram?: Address<TAccountSystemProgram>;
  /** Token */
  tokenProgram?: Address<TAccountTokenProgram>;
  mint: Address<TAccountMint>;
  gameTokenAccount: Address<TAccountGameTokenAccount>;
  playerTokenAccount: Address<TAccountPlayerTokenAccount>;
  amount: AddFundsInstructionDataArgs["amount"];
};

export function getAddFundsInstruction<
  TAccountGame extends string,
  TAccountSigner extends string,
  TAccountSystemProgram extends string,
  TAccountTokenProgram extends string,
  TAccountMint extends string,
  TAccountGameTokenAccount extends string,
  TAccountPlayerTokenAccount extends string,
  TProgramAddress extends Address = typeof HORMUZ_RUN_PROGRAM_ADDRESS,
>(
  input: AddFundsInput<
    TAccountGame,
    TAccountSigner,
    TAccountSystemProgram,
    TAccountTokenProgram,
    TAccountMint,
    TAccountGameTokenAccount,
    TAccountPlayerTokenAccount
  >,
  config?: { programAddress?: TProgramAddress },
): AddFundsInstruction<
  TProgramAddress,
  TAccountGame,
  TAccountSigner,
  TAccountSystemProgram,
  TAccountTokenProgram,
  TAccountMint,
  TAccountGameTokenAccount,
  TAccountPlayerTokenAccount
> {
  // Program address.
  const programAddress = config?.programAddress ?? HORMUZ_RUN_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    game: { value: input.game ?? null, isWritable: true },
    signer: { value: input.signer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    mint: { value: input.mint ?? null, isWritable: true },
    gameTokenAccount: {
      value: input.gameTokenAccount ?? null,
      isWritable: true,
    },
    playerTokenAccount: {
      value: input.playerTokenAccount ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("game", accounts.game),
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("systemProgram", accounts.systemProgram),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("mint", accounts.mint),
      getAccountMeta("gameTokenAccount", accounts.gameTokenAccount),
      getAccountMeta("playerTokenAccount", accounts.playerTokenAccount),
    ],
    data: getAddFundsInstructionDataEncoder().encode(
      args as AddFundsInstructionDataArgs,
    ),
    programAddress,
  } as AddFundsInstruction<
    TProgramAddress,
    TAccountGame,
    TAccountSigner,
    TAccountSystemProgram,
    TAccountTokenProgram,
    TAccountMint,
    TAccountGameTokenAccount,
    TAccountPlayerTokenAccount
  >);
}

export type ParsedAddFundsInstruction<
  TProgram extends string = typeof HORMUZ_RUN_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    game: TAccountMetas[0];
    signer: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
    /** Token */
    tokenProgram: TAccountMetas[3];
    mint: TAccountMetas[4];
    gameTokenAccount: TAccountMetas[5];
    playerTokenAccount: TAccountMetas[6];
  };
  data: AddFundsInstructionData;
};

export function parseAddFundsInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAddFundsInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 7,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      game: getNextAccount(),
      signer: getNextAccount(),
      systemProgram: getNextAccount(),
      tokenProgram: getNextAccount(),
      mint: getNextAccount(),
      gameTokenAccount: getNextAccount(),
      playerTokenAccount: getNextAccount(),
    },
    data: getAddFundsInstructionDataDecoder().decode(instruction.data),
  };
}
