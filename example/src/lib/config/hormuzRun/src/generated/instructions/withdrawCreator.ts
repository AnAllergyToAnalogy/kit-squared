/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findGamePda } from "../pdas";
import { HORMUZ_RUN_PROGRAM_ADDRESS } from "../programs";

export const WITHDRAW_CREATOR_DISCRIMINATOR = new Uint8Array([
  216, 134, 243, 253, 80, 5, 67, 57,
]);

export function getWithdrawCreatorDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    WITHDRAW_CREATOR_DISCRIMINATOR,
  );
}

export type WithdrawCreatorInstruction<
  TProgram extends string = typeof HORMUZ_RUN_PROGRAM_ADDRESS,
  TAccountGame extends string | AccountMeta<string> = string,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountGameTokenAccount extends string | AccountMeta<string> = string,
  TAccountRecipientTokenAccount extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountGame extends string
        ? WritableAccount<TAccountGame>
        : TAccountGame,
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      TAccountMint extends string
        ? WritableAccount<TAccountMint>
        : TAccountMint,
      TAccountGameTokenAccount extends string
        ? WritableAccount<TAccountGameTokenAccount>
        : TAccountGameTokenAccount,
      TAccountRecipientTokenAccount extends string
        ? WritableAccount<TAccountRecipientTokenAccount>
        : TAccountRecipientTokenAccount,
      ...TRemainingAccounts,
    ]
  >;

export type WithdrawCreatorInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
};

export type WithdrawCreatorInstructionDataArgs = { amount: number | bigint };

export function getWithdrawCreatorInstructionDataEncoder(): FixedSizeEncoder<WithdrawCreatorInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amount", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: WITHDRAW_CREATOR_DISCRIMINATOR }),
  );
}

export function getWithdrawCreatorInstructionDataDecoder(): FixedSizeDecoder<WithdrawCreatorInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amount", getU64Decoder()],
  ]);
}

export function getWithdrawCreatorInstructionDataCodec(): FixedSizeCodec<
  WithdrawCreatorInstructionDataArgs,
  WithdrawCreatorInstructionData
> {
  return combineCodec(
    getWithdrawCreatorInstructionDataEncoder(),
    getWithdrawCreatorInstructionDataDecoder(),
  );
}

export type WithdrawCreatorAsyncInput<
  TAccountGame extends string = string,
  TAccountSigner extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountMint extends string = string,
  TAccountGameTokenAccount extends string = string,
  TAccountRecipientTokenAccount extends string = string,
> = {
  game?: Address<TAccountGame>;
  signer: TransactionSigner<TAccountSigner>;
  systemProgram?: Address<TAccountSystemProgram>;
  /** Token */
  tokenProgram?: Address<TAccountTokenProgram>;
  mint: Address<TAccountMint>;
  gameTokenAccount: Address<TAccountGameTokenAccount>;
  recipientTokenAccount: Address<TAccountRecipientTokenAccount>;
  amount: WithdrawCreatorInstructionDataArgs["amount"];
};

export async function getWithdrawCreatorInstructionAsync<
  TAccountGame extends string,
  TAccountSigner extends string,
  TAccountSystemProgram extends string,
  TAccountTokenProgram extends string,
  TAccountMint extends string,
  TAccountGameTokenAccount extends string,
  TAccountRecipientTokenAccount extends string,
  TProgramAddress extends Address = typeof HORMUZ_RUN_PROGRAM_ADDRESS,
>(
  input: WithdrawCreatorAsyncInput<
    TAccountGame,
    TAccountSigner,
    TAccountSystemProgram,
    TAccountTokenProgram,
    TAccountMint,
    TAccountGameTokenAccount,
    TAccountRecipientTokenAccount
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  WithdrawCreatorInstruction<
    TProgramAddress,
    TAccountGame,
    TAccountSigner,
    TAccountSystemProgram,
    TAccountTokenProgram,
    TAccountMint,
    TAccountGameTokenAccount,
    TAccountRecipientTokenAccount
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? HORMUZ_RUN_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    game: { value: input.game ?? null, isWritable: true },
    signer: { value: input.signer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    mint: { value: input.mint ?? null, isWritable: true },
    gameTokenAccount: {
      value: input.gameTokenAccount ?? null,
      isWritable: true,
    },
    recipientTokenAccount: {
      value: input.recipientTokenAccount ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.game.value) {
    accounts.game.value = await findGamePda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("game", accounts.game),
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("systemProgram", accounts.systemProgram),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("mint", accounts.mint),
      getAccountMeta("gameTokenAccount", accounts.gameTokenAccount),
      getAccountMeta("recipientTokenAccount", accounts.recipientTokenAccount),
    ],
    data: getWithdrawCreatorInstructionDataEncoder().encode(
      args as WithdrawCreatorInstructionDataArgs,
    ),
    programAddress,
  } as WithdrawCreatorInstruction<
    TProgramAddress,
    TAccountGame,
    TAccountSigner,
    TAccountSystemProgram,
    TAccountTokenProgram,
    TAccountMint,
    TAccountGameTokenAccount,
    TAccountRecipientTokenAccount
  >);
}

export type WithdrawCreatorInput<
  TAccountGame extends string = string,
  TAccountSigner extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountMint extends string = string,
  TAccountGameTokenAccount extends string = string,
  TAccountRecipientTokenAccount extends string = string,
> = {
  game: Address<TAccountGame>;
  signer: TransactionSigner<TAccountSigner>;
  systemProgram?: Address<TAccountSystemProgram>;
  /** Token */
  tokenProgram?: Address<TAccountTokenProgram>;
  mint: Address<TAccountMint>;
  gameTokenAccount: Address<TAccountGameTokenAccount>;
  recipientTokenAccount: Address<TAccountRecipientTokenAccount>;
  amount: WithdrawCreatorInstructionDataArgs["amount"];
};

export function getWithdrawCreatorInstruction<
  TAccountGame extends string,
  TAccountSigner extends string,
  TAccountSystemProgram extends string,
  TAccountTokenProgram extends string,
  TAccountMint extends string,
  TAccountGameTokenAccount extends string,
  TAccountRecipientTokenAccount extends string,
  TProgramAddress extends Address = typeof HORMUZ_RUN_PROGRAM_ADDRESS,
>(
  input: WithdrawCreatorInput<
    TAccountGame,
    TAccountSigner,
    TAccountSystemProgram,
    TAccountTokenProgram,
    TAccountMint,
    TAccountGameTokenAccount,
    TAccountRecipientTokenAccount
  >,
  config?: { programAddress?: TProgramAddress },
): WithdrawCreatorInstruction<
  TProgramAddress,
  TAccountGame,
  TAccountSigner,
  TAccountSystemProgram,
  TAccountTokenProgram,
  TAccountMint,
  TAccountGameTokenAccount,
  TAccountRecipientTokenAccount
> {
  // Program address.
  const programAddress = config?.programAddress ?? HORMUZ_RUN_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    game: { value: input.game ?? null, isWritable: true },
    signer: { value: input.signer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    mint: { value: input.mint ?? null, isWritable: true },
    gameTokenAccount: {
      value: input.gameTokenAccount ?? null,
      isWritable: true,
    },
    recipientTokenAccount: {
      value: input.recipientTokenAccount ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("game", accounts.game),
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("systemProgram", accounts.systemProgram),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("mint", accounts.mint),
      getAccountMeta("gameTokenAccount", accounts.gameTokenAccount),
      getAccountMeta("recipientTokenAccount", accounts.recipientTokenAccount),
    ],
    data: getWithdrawCreatorInstructionDataEncoder().encode(
      args as WithdrawCreatorInstructionDataArgs,
    ),
    programAddress,
  } as WithdrawCreatorInstruction<
    TProgramAddress,
    TAccountGame,
    TAccountSigner,
    TAccountSystemProgram,
    TAccountTokenProgram,
    TAccountMint,
    TAccountGameTokenAccount,
    TAccountRecipientTokenAccount
  >);
}

export type ParsedWithdrawCreatorInstruction<
  TProgram extends string = typeof HORMUZ_RUN_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    game: TAccountMetas[0];
    signer: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
    /** Token */
    tokenProgram: TAccountMetas[3];
    mint: TAccountMetas[4];
    gameTokenAccount: TAccountMetas[5];
    recipientTokenAccount: TAccountMetas[6];
  };
  data: WithdrawCreatorInstructionData;
};

export function parseWithdrawCreatorInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedWithdrawCreatorInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 7,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      game: getNextAccount(),
      signer: getNextAccount(),
      systemProgram: getNextAccount(),
      tokenProgram: getNextAccount(),
      mint: getNextAccount(),
      gameTokenAccount: getNextAccount(),
      recipientTokenAccount: getNextAccount(),
    },
    data: getWithdrawCreatorInstructionDataDecoder().decode(instruction.data),
  };
}
