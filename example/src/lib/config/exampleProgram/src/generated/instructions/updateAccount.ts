/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { EXAMPLE_PROGRAM_PROGRAM_ADDRESS } from "../programs";

export const UPDATE_ACCOUNT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  231, 31, 72, 97, 68, 133, 133, 152,
]);

export function getUpdateAccountDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_ACCOUNT_DISCRIMINATOR,
  );
}

export type UpdateAccountInstruction<
  TProgram extends string = typeof EXAMPLE_PROGRAM_PROGRAM_ADDRESS,
  TAccountToUpdate extends string | AccountMeta<string> = string,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountToUpdate extends string
        ? WritableAccount<TAccountToUpdate>
        : TAccountToUpdate,
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateAccountInstructionData = {
  discriminator: ReadonlyUint8Array;
  someU32: number;
  someU64: bigint;
  someBool: boolean;
};

export type UpdateAccountInstructionDataArgs = {
  someU32: number;
  someU64: number | bigint;
  someBool: boolean;
};

export function getUpdateAccountInstructionDataEncoder(): FixedSizeEncoder<UpdateAccountInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["someU32", getU32Encoder()],
      ["someU64", getU64Encoder()],
      ["someBool", getBooleanEncoder()],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_ACCOUNT_DISCRIMINATOR }),
  );
}

export function getUpdateAccountInstructionDataDecoder(): FixedSizeDecoder<UpdateAccountInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["someU32", getU32Decoder()],
    ["someU64", getU64Decoder()],
    ["someBool", getBooleanDecoder()],
  ]);
}

export function getUpdateAccountInstructionDataCodec(): FixedSizeCodec<
  UpdateAccountInstructionDataArgs,
  UpdateAccountInstructionData
> {
  return combineCodec(
    getUpdateAccountInstructionDataEncoder(),
    getUpdateAccountInstructionDataDecoder(),
  );
}

export type UpdateAccountInput<
  TAccountToUpdate extends string = string,
  TAccountSigner extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  toUpdate: Address<TAccountToUpdate>;
  signer: TransactionSigner<TAccountSigner>;
  systemProgram?: Address<TAccountSystemProgram>;
  someU32: UpdateAccountInstructionDataArgs["someU32"];
  someU64: UpdateAccountInstructionDataArgs["someU64"];
  someBool: UpdateAccountInstructionDataArgs["someBool"];
};

export function getUpdateAccountInstruction<
  TAccountToUpdate extends string,
  TAccountSigner extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof EXAMPLE_PROGRAM_PROGRAM_ADDRESS,
>(
  input: UpdateAccountInput<
    TAccountToUpdate,
    TAccountSigner,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): UpdateAccountInstruction<
  TProgramAddress,
  TAccountToUpdate,
  TAccountSigner,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? EXAMPLE_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    toUpdate: { value: input.toUpdate ?? null, isWritable: true },
    signer: { value: input.signer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("toUpdate", accounts.toUpdate),
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getUpdateAccountInstructionDataEncoder().encode(
      args as UpdateAccountInstructionDataArgs,
    ),
    programAddress,
  } as UpdateAccountInstruction<
    TProgramAddress,
    TAccountToUpdate,
    TAccountSigner,
    TAccountSystemProgram
  >);
}

export type ParsedUpdateAccountInstruction<
  TProgram extends string = typeof EXAMPLE_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    toUpdate: TAccountMetas[0];
    signer: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: UpdateAccountInstructionData;
};

export function parseUpdateAccountInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateAccountInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      toUpdate: getNextAccount(),
      signer: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getUpdateAccountInstructionDataDecoder().decode(instruction.data),
  };
}
