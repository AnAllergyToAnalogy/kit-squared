/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const SOME_ACCOUNT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  211, 98, 44, 55, 84, 126, 187, 236,
]);

export function getSomeAccountDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SOME_ACCOUNT_DISCRIMINATOR,
  );
}

export type SomeAccount = {
  discriminator: ReadonlyUint8Array;
  someKey: Address;
  someU32: number;
  someU64: bigint;
  someBool: boolean;
};

export type SomeAccountArgs = {
  someKey: Address;
  someU32: number;
  someU64: number | bigint;
  someBool: boolean;
};

/** Gets the encoder for {@link SomeAccountArgs} account data. */
export function getSomeAccountEncoder(): FixedSizeEncoder<SomeAccountArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["someKey", getAddressEncoder()],
      ["someU32", getU32Encoder()],
      ["someU64", getU64Encoder()],
      ["someBool", getBooleanEncoder()],
    ]),
    (value) => ({ ...value, discriminator: SOME_ACCOUNT_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link SomeAccount} account data. */
export function getSomeAccountDecoder(): FixedSizeDecoder<SomeAccount> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["someKey", getAddressDecoder()],
    ["someU32", getU32Decoder()],
    ["someU64", getU64Decoder()],
    ["someBool", getBooleanDecoder()],
  ]);
}

/** Gets the codec for {@link SomeAccount} account data. */
export function getSomeAccountCodec(): FixedSizeCodec<
  SomeAccountArgs,
  SomeAccount
> {
  return combineCodec(getSomeAccountEncoder(), getSomeAccountDecoder());
}

export function decodeSomeAccount<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<SomeAccount, TAddress>;
export function decodeSomeAccount<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<SomeAccount, TAddress>;
export function decodeSomeAccount<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<SomeAccount, TAddress> | MaybeAccount<SomeAccount, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getSomeAccountDecoder(),
  );
}

export async function fetchSomeAccount<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<SomeAccount, TAddress>> {
  const maybeAccount = await fetchMaybeSomeAccount(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeSomeAccount<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<SomeAccount, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeSomeAccount(maybeAccount);
}

export async function fetchAllSomeAccount(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<SomeAccount>[]> {
  const maybeAccounts = await fetchAllMaybeSomeAccount(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeSomeAccount(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<SomeAccount>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeSomeAccount(maybeAccount));
}

export function getSomeAccountSize(): number {
  return 53;
}
